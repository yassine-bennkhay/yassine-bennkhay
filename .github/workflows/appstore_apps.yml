name: Update iOS Apps in README
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 */2 * *'  # Runs every 2 days

permissions:
  contents: write

jobs:
  update-readme-with-apps:
    name: Update README with latest iOS apps
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Fetch iOS apps from App Store
        run: |
         
          APP_LIST=$(curl -s "https://itunes.apple.com/lookup?id=1769430215&entity=software" | jq -r '.results[] | "- [" + .trackName + "](" + .trackViewUrl + ")"')
          
          # Insert the app list into README.md where the placeholder is
          sed -i '/<!-- APP-STORE-LIST-START -->/,/<!-- APP-STORE-LIST-END -->/c\\n<!-- APP-STORE-LIST-START -->\n'"${APP_LIST}"'\n<!-- APP-STORE-LIST-END -->' README.md

      - name: Commit changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          git commit -m "Update README with latest apps from App Store"
          git push
